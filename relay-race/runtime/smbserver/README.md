# smbserver — Relay Race

Share SMB (impacket) con NTLM. El fichero Payroll_Q4_2026.txt lleva la flag,
leida de `seed/share_token.txt`. En el repo es un **DECOY**; el token real se
inyecta en CI desde el secret `RELAY_RACE_TOKEN`.
