# Changelog — Full Circle

## 1.0.0
- Lab capstone: kill chain multifase en un solo PCAP (Avanzado).
- Escenario Northwind: recon (escaneo) -> credential access (login HTTP en claro)
  -> exfil (descarga de export sensible con la flag).
- 3 contenedores: target (servidor victima), attacker (kill chain), sniffer.
- 6 preguntas (fase 1, usuario, contrasena, fichero, tecnica ATT&CK, flag).
- Evidencia: pcaps/kill-chain.pcap.
