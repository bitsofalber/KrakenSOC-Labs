# target — Full Circle

Servidor con puertos abiertos (recon), login HTTP en claro y un export sensible
tras sesion. La flag va en el export, leida de `seed/export_token.txt`. En el repo
es un **DECOY**; el token real se inyecta en CI desde `FULL_CIRCLE_TOKEN`.
